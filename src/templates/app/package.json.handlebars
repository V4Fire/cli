{{! output_name=package ext=json }}
{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "Description {{name}}",
  "scripts": {
    "build": "gulp build:tsconfig && webpack --watch --debug false",
    "start": "node webview.app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@YourFullName",
  "keywords": [
    "{{name}}"
  ],
  "dependencies": {
    "@v4fire/client": "^3.0.0-rc.59",
    "@v4fire/core": "^3.24.0",
    "@v4fire/linters": "^1.5.4",
    "express": "^4.17.1",
    "express-http-proxy": "^1.6.2",
    "parallel-webpack": "^2.6.0"
  },
  "license": "MIT"
}
